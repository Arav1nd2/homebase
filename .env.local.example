# Copy to .env.local for local development. `supabase start` prints these
# values (or use its defaults below, which match a fresh `supabase init`).
DATABASE_URL="postgresql://postgres:postgres@localhost:54322/postgres"
